/*
    Luke Peterson
    8/23/2019
    
    Taking the given string and using parseInt on str to change it into an integer, and also takes the base value and converts it into that base.  Here we are using binary so 2 is the value given.
*/

function convertToInteger(str) {
  return parseInt(str, 2);
}

convertToInteger("10011");