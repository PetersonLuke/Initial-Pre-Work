/*
    Luke Peterson
    8/21/2019
    
    Simplified the code to just return if a is less than or equal to b, instead of writing out an if statement to check.
*/

function isLess(a, b) {
  // Fix this code
  return a <= b;
}

// Change these values to test
isLess(10, 15);