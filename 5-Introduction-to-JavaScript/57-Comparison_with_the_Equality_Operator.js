/*
    Luke Peterson
    8/21/2019
    
    Checking to see if val is equal to 12 by using ==.  If it is "Equal is returned, if not "Not Equal is returned.
*/

// Setup
function testEqual(val) {
  if (val == 12) { // Change this line
    return "Equal";
  }
  return "Not Equal";
}

// Change this value to test
testEqual(10);