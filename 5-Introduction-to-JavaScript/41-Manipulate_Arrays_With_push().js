/*
    Luke Peterson
    8/20/2019
    
    Pushed the string dog and the integer 3 to the array myArray.  This will add "dog", 3 to the end of the array.
*/

// Example
var ourArray = ["Stimpson", "J", "cat"];
ourArray.push(["happy", "joy"]); 
// ourArray now equals ["Stimpson", "J", "cat", ["happy", "joy"]]

// Setup
var myArray = [["John", 23], ["cat", 2]];

// Only change code below this line.
myArray.push(["dog", 3]);