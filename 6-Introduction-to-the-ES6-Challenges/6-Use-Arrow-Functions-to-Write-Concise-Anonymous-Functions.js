/*
    Luke Peterson
    8/23/2019
    
    Used an arrow function (=>) because the function here is not used anywhere else, so it doesn't need a name.
*/

const magic = () =>{
  "use strict";
  return new Date();
};